import React from "react";
import ReactDOM from "react-dom";

//JSX => facebook developers built react
//JSX => React.createElement => Object => HTML(DOM)
// i.e. jsx convert code to react.createElement then it convert to object then object to html then html to dom

//JSX convets code to React.createElement by the help of babel 

//this is not html inside javascript
//this is html like syntax . this is fancy way of wirting html inside js but it is not html
const heading = (
    <h1 id="title" key='h1'> Namaste React </h1>
);
